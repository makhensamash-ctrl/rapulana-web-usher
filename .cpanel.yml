---
deployment:
  tasks:
    - npm install
    - npm run build
    - export DEPLOYPATH=/home/rapulana/public_html
    - /bin/rm -rf "$DEPLOYPATH"/*
    - /bin/cp -R dist/* "$DEPLOYPATH"
    - if [ -f public/.htaccess ]; then /bin/cp public/.htaccess "$DEPLOYPATH"/.htaccess; fi